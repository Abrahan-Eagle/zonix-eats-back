## Overlay Zonix Glasses — task-pipeline-ops

Plan → Spec → Exec → Verify → Fix (máx. 3) para features óptica.

Verificación: `php artisan test --filter=Prescription` (cuando exista) + `check-global-skills-sync.sh`.
