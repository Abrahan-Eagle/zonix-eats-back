## Overlay Zonix Glasses — git-guardrails-ops

- `dev` → pruebas; `main` → producción
- **Nunca** push/merge sin OK explícito del usuario
- No force-push a `main`
- Commits locales solo cuando el usuario lo pida

Remote: debe apuntar a repo Zonix Glasses (no `zonix-eats-back`).
