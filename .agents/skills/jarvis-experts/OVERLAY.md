## Overlay Zonix Glasses — jarvis-experts

Declarar 1–3 roles en español: `> Roles: backend (Laravel) + PM` o `> Roles: AppSec + privacidad`.

Combinaciones frecuentes:
- API óptica / fórmulas: backend + AppSec + DBA
- Modelo negocio / docs: PM + legal consultivo + UX writer
- Try-on IA: ML eng + frontend + privacidad

Dominio: skills `zonix-glasses-*` en `.agents/skills/`.
