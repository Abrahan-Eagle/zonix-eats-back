## Overlay Zonix Glasses — structured-commits-ops

Trailers útiles: `Decision:`, `Domain: optical|prescription|tryon`, `Refs: specs/001-*`.

Un propósito por commit; tests/docs en el mismo commit que el código.
