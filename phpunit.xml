<?xml version="1.0" encoding="UTF-8"?>
<phpunit xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:noNamespaceSchemaLocation="vendor/phpunit/phpunit/phpunit.xsd"
         bootstrap="vendor/autoload.php"
         colors="true"
>
    <testsuites>
        <testsuite name="Unit">
            <directory>tests/Unit</directory>
        </testsuite>
        <testsuite name="Feature">
            <directory>tests/Feature</directory>
        </testsuite>
    </testsuites>
    <source>
        <include>
            <directory>app</directory>
        </include>
    </source>
    <php>
        <env name="APP_ENV" value="testing"/>
        <!-- Evita HTTP real a Pusher en la suite (órdenes/eventos → 500/timeout con credenciales dummy).
             PusherConfigTest y WebSocketTest (auth) fijan credenciales/driver solo donde hace falta. -->
        <env name="BROADCAST_DRIVER" value="null"/>
        <!-- Valores para asserts de config/env en PusherConfigTest (no activan broadcast con driver null) -->
        <env name="PUSHER_APP_ID" value="test-app-id"/>
        <env name="PUSHER_APP_KEY" value="test-key"/>
        <env name="PUSHER_APP_SECRET" value="test-secret"/>
        <env name="PUSHER_APP_CLUSTER" value="mt1"/>
        <env name="BCRYPT_ROUNDS" value="4"/>
        <env name="CACHE_DRIVER" value="array"/>
        <!-- Tests con SQLite en memoria (no requiere MySQL). Para usar MySQL, defina DB_CONNECTION=mysql en .env.testing -->
        <env name="DB_CONNECTION" value="sqlite"/>
        <env name="DB_DATABASE" value=":memory:"/>
        <env name="MAIL_MAILER" value="array"/>
        <env name="PULSE_ENABLED" value="false"/>
        <env name="QUEUE_CONNECTION" value="sync"/>
        <env name="SESSION_DRIVER" value="array"/>
        <env name="TELESCOPE_ENABLED" value="false"/>
        <!-- Solo escribir error+ en log durante tests; evita ruido de INFO/DEBUG -->
        <env name="LOG_LEVEL" value="error"/>
        <!-- Caducidad pending_payment: explícito para que la suite no dependa del .env del desarrollador -->
        <env name="ZONIX_EXPIRE_PENDING_PAYMENT_ENABLED" value="true"/>
        <env name="ZONIX_PENDING_PAYMENT_MAX_AGE_MINUTES" value="1440"/>
        <env name="ZONIX_PENDING_PAYMENT_AFTER_APPROVAL_MINUTES" value="60"/>
        <env name="ZONIX_EXPIRE_SKIP_IF_PROOF_PENDING" value="true"/>
    </php>
</phpunit>
