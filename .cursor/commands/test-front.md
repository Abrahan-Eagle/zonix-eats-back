# Run Frontend Tests

Execute the Flutter test suite for zonix-glasses-front.

## Instructions

1. If current workspace is `zonix-glasses-front`: run `flutter test`
2. If current workspace is `zonix-glasses-back`: run `flutter test` from `../zonix-glasses-front`
